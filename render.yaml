services:
  - type: web
    name: telegram-ocr-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: API_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
